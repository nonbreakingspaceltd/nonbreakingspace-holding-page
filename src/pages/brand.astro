---
// Thin route entry for /brand. The page owns nothing but the URL — the whole
// composition (layout, the thirteen spec sections, scoped styles) lives in the
// Brand view, mirroring the pages → views → components split.
import Brand from '../views/Brand';
---

<Brand />
