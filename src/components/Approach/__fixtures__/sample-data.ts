// The verbs the Approach section is expected to render, in order. Kept here so
// the test guards against an accidental step being dropped or renamed.
export const expectedSteps = ['Think', 'Shape', 'Make'] as const;
