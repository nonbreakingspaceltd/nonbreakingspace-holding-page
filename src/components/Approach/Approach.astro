---
import './Approach.css';
import SectionLabel from '../SectionLabel';

// Section 04 — the page's single recessed tonal band. Three framed plates on
// one cream wall: Think / Shape / Make.
const steps = [
  {
    title: 'Think',
    body: 'Understand the problem, the people it serves and the limits it must work within.',
  },
  {
    title: 'Shape',
    body: 'Define its structure and visual language until every part holds together.',
  },
  {
    title: 'Make',
    body: 'Build it to last — considered, accessible to everyone and crafted with passion.',
  },
];
---

<section id="approach" class="approach" aria-labelledby="approach-heading">
  <div class="wrap">
    <SectionLabel index="03" label="Approach" id="approach-heading" />

    <div class="approach__grid">
      {
        steps.map((step, i) => (
          <article class="approach__col" data-reveal style={`--reveal-delay: ${i * 80}ms`}>
            <h3 class="t-heading approach__verb">{step.title}</h3>
            <p class="t-body approach__copy">{step.body}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>
