// Display scale class tokens, in source order — the value rendered under each
// Fraunces specimen (e.g. `.t-cta`).
export const expectedDisplayClasses = [
  '.t-cta',
  '.t-hero',
  '.t-statement',
  '.t-heading',
  '.t-cap',
  '.t-email',
];

// Functional role names, in source order, from the functionalType array.
export const expectedRoles = ['Body', 'Eyebrow', 'Meta label', 'Row index', 'Legal'];
